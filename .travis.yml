language: java
sudo: true

# This is needed to link against the correct opencv
before_install:
  - sudo apt-get -qq update
  - sudo apt-get -qq install libstdc++6 libgcc1
  - sudo ldconfig

jdk:
  - oraclejdk8